# Example environment configuration for npmp-cli

# Base URL to your NPMplus instance (without /api is fine)
NPMP_BASE_URL=https://npmproxy:81

# Auth (required)
NPMP_IDENTITY=bot
NPMP_SECRET=changeme

# Logging
NPMP_LOG_LEVEL=INFO
# NPMP_LOG_CONSOLE_STREAM=stdout

# HTTP retries (transient disconnect/transport errors)
# Total attempts = NPMP_HTTP_RETRY_COUNT + 1
# NPMP_HTTP_RETRY_COUNT=3

# Optional file logging
# If set to an existing directory or a path ending with '/', logs to <dir>/npmp-cli.log
# NPMP_LOG_FILE=./npmp-cli.log
# NPMP_LOG_FILE=./logs/

# Optional: separate file logging level (defaults to NPMP_LOG_LEVEL)
# NPMP_LOG_FILE_LEVEL=DEBUG

# TLS verification
# Set to false to disable certificate verification.
NPMP_VERIFY_TLS=false

# Docker sync
# Prefix for docker labels (must end with '.')
NPMP_DOCKER_LABEL_PREFIX=npmp.

# Docker connection (optional)
# If not set, defaults to local Docker socket (unix:///var/run/docker.sock)
# DOCKER_HOST=tcp://remote-host:2375

# Optional: use a different env file by setting NPMP_ENV_FILE in your shell before running npmp-cli.
# Example:
#   export NPMP_ENV_FILE=.env.prod
